[project]
name = "clawguard-demo"
version = "0.1.0"
description = "ClawGuard agent security evaluation harness"
readme = "README.md"
requires-python = ">=3.10"

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
